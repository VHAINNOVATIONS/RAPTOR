#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ntp-client/bin/node-ntp-client" "$@"
  ret=$?
else 
  node  "$basedir/../ntp-client/bin/node-ntp-client" "$@"
  ret=$?
fi
exit $ret
